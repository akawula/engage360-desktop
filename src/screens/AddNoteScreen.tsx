/**
 * AddNoteScreen - Legacy screen component for adding notes
 * This component exists for consistency with the screen pattern used in React Native
 * For web use, the main CreateNote page component should be preferred
 */

import CreateNote from '../pages/CreateNote';

export default function AddNoteScreen() {
    return <CreateNote />;
}
